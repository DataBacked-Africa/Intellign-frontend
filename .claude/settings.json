{
  "permissions": {
    "allow": [
      "WebFetch(domain:getcertin.ai)",
      "Bash(timeout 30 bash -c 'until curl -sf http://localhost:3000 >/dev/null || curl -sf http://localhost:3001 >/dev/null; do sleep 1; done')",
      "Read(//tmp/**)"
    ]
  }
}
